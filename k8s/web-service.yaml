apiVersion: v1
kind: Service
metadata:
  name: fitnesstracker-web
  namespace: fitnesstracker
spec:
  selector:
    app: fitnesstracker-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
